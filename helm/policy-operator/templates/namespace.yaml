{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "policy-operator.namespace" . }}
  labels:
    control-plane: controller-manager
    {{- include "policy-operator.labels" . | nindent 4 }}
{{- end }}
