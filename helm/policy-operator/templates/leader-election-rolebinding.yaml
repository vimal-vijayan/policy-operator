apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "policy-operator.fullname" . }}-leader-election-rolebinding
  namespace: {{ include "policy-operator.namespace" . }}
  labels:
    {{- include "policy-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "policy-operator.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "policy-operator.serviceAccountName" . }}
  namespace: {{ include "policy-operator.namespace" . }}
