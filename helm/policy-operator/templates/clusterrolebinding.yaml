apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "policy-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "policy-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "policy-operator.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "policy-operator.serviceAccountName" . }}
  namespace: {{ include "policy-operator.namespace" . }}
