---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: azurepolicydefinitions.governance.platform.io
spec:
  group: governance.platform.io
  names:
    kind: AzurePolicyDefinition
    listKind: AzurePolicyDefinitionList
    plural: azurepolicydefinitions
    singular: azurepolicydefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.policyDefinitionId
      name: ID
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AzurePolicyDefinition is the Schema for the azurepolicydefinitions
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AzurePolicyDefinitionSpec defines the desired state of AzurePolicyDefinition
            properties:
              description:
                description: Description is a human-readable description of the policy
                  definition.
                type: string
              displayName:
                description: DisplayName is the display name of the policy definition.
                type: string
              managementGroupId:
                description: ManagementGroupID is the management group scope to deploy
                  the policy definition to.
                type: string
              metadata:
                description: Metadata is additional metadata for the policy definition
                  as a raw JSON object.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              mode:
                description: 'Mode determines which resource types are evaluated.
                  Common values: All, Indexed.'
                type: string
              parameters:
                description: Parameters defines the parameters that can be used in
                  the policy rule.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              policyRule:
                description: PolicyRule is the logic of the policy definition as a
                  raw JSON object.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              policyRuleJson:
                description: |-
                  PolicyRuleJSON is the logic of the policy definition as a JSON string.
                  This is useful when managing very large policy definitions as raw JSON text.
                type: string
              policyType:
                default: Custom
                description: 'PolicyType is the type of policy definition. Allowed
                  values: BuiltIn, Custom, NotSpecified, Static.'
                enum:
                - BuiltIn
                - Custom
                - NotSpecified
                - Static
                type: string
              subscriptionId:
                description: |-
                  SubscriptionID is the Azure subscription to deploy the policy definition to.
                  If omitted, the policy is created at the management group scope.
                type: string
              version:
                description: |-
                  Version is the semantic version of the policy definition (e.g. "1.0.0").
                  When set, it is injected into the Azure Policy metadata object under the
                  key "version", which is displayed by the Azure portal. spec.version always
                  takes precedence over any "version" key already present in spec.metadata.
                pattern: ^\d+\.\d+\.\d+$
                type: string
            required:
            - displayName
            - mode
            type: object
            x-kubernetes-validations:
            - message: Exactly one of policyRule or policyRuleJson must be specified.
              rule: has(self.policyRule) != has(self.policyRuleJson)
          status:
            description: AzurePolicyDefinitionStatus defines the observed state of
              AzurePolicyDefinition
            properties:
              appliedVersion:
                description: |-
                  AppliedVersion is the semver version last successfully written to Azure
                  Policy metadata. Mirrors spec.version after each successful reconcile.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the resource state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              policyDefinitionId:
                description: PolicyDefinitionID is the Azure resource ID of the created
                  policy definition.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
