# CI Pipeline — Feature Branches
# Triggers on any branch matching feat/* or fix/* or chore/*
# Runs: fmt check, vet, unit tests, build

trigger:
  branches:
    include:
      - feat/*
      - fix/*
      - chore/*
  paths:
    exclude:
      - "*.md"
      - docs/**
      - azurepipelines/**

pr: none

variables:
  GO_VERSION: "1.24.x"
  ENVTEST_K8S_VERSION: "1.31.0"

pool:
  vmImage: ubuntu-latest

stages:
  - stage: CI
    displayName: "CI — Feature Branch"
    jobs:
      - job: Build
        displayName: "Fmt / Vet / Test / Build"
        steps:
          - task: GoTool@0
            displayName: "Install Go $(GO_VERSION)"
            inputs:
              version: $(GO_VERSION)

          - script: |
              go mod download
            displayName: "Download dependencies"
            workingDirectory: $(System.DefaultWorkingDirectory)

          - script: |
              if [ -n "$(gofmt -l .)" ]; then
                echo "The following files are not gofmt-compliant:"
                gofmt -l .
                exit 1
              fi
            displayName: "go fmt check"
            workingDirectory: $(System.DefaultWorkingDirectory)

          - script: |
              go vet ./...
            displayName: "go vet"
            workingDirectory: $(System.DefaultWorkingDirectory)

          - script: |
              make envtest
              KUBEBUILDER_ASSETS="$(./bin/setup-envtest use $(ENVTEST_K8S_VERSION) --bin-dir ./bin -p path)" \
                go test ./... -v -coverprofile=cover.out
            displayName: "go test"
            workingDirectory: $(System.DefaultWorkingDirectory)
            env:
              ENVTEST_K8S_VERSION: $(ENVTEST_K8S_VERSION)

          - script: |
              CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /dev/null ./cmd/main.go
            displayName: "go build"
            workingDirectory: $(System.DefaultWorkingDirectory)

          - task: PublishCodeCoverageResults@2
            displayName: "Publish coverage"
            condition: succeededOrFailed()
            inputs:
              summaryFileLocation: "$(System.DefaultWorkingDirectory)/cover.out"
